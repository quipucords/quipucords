"""quipucords mock setup."""

from setuptools import setup

setup(
    name="quipucords",
    # version is just a placeholder version (cachito requires it). The proper version is
    # in pyproject.toml
    version="0.0.0",
)
