"""quipucords mock setup."""

from setuptools import setup

setup(
    name="quipucords",
    version="0.0.0",  # just a placeholder version (cachito requires it)
)

raise NotImplementedError(
    "Quipucords SHOULD NOT be installed from setup.py. This file only exists to please "
    "an archaic downstream build system."
)
