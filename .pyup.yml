# autogenerated pyup.io config file
# see https://pyup.io/docs/configuration/ for all available options

schedule: every week

# specify requirements files to update
requirements:
  - dev-requirements.txt:
      # update all dependencies and pin them
      update: all
      pin: True
  - dev-locked-requirements.txt:
      # do not update locked dependencies
      update: False
  - requirements.txt:
      # update all dependencies and pin them
      update: all
      pin: True
  - locked-requirements.txt:
      # do not update locked dependencies
      update: False
